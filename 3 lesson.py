
# Задача №1 (Раздел: "Удаление элементов из списка и его очистка")
# Дан список: items = ["стол", "стул", "лампа", "книга", "чашка"]
# Удали из списка элемент "лампа" по его значению.
# Удали из списка элемент, который находится на позиции с индексом 1.
# После этого очисти весь список, сделав его пустым.
# Выведи итоговый список после всех операций, чтобы убедиться, что он пуст.

items = ["стол", "стул", "лампа", "книга", "чашка"]
items.remove("лампа")
del items[1]
items.clear()
print(items)

# Задача №2 (Раздел: "Создание списка и получение элементов")
# Вручную создай список с названием colors, который содержит следующие строки:
# "красный", "зеленый", "синий", "желтый".
# Выведи на экран (с помощью print) первый элемент этого списка.
# Выведи на экран третий элемент этого списка.
# Выведи на экран последний элемент этого списка, используя отрицательную индексацию.

colors = ["красный", "зеленый", "синий", "желтый"]
print(colors[0])
print(colors[2])
print(colors[-1])

# Задача №3 (Раздел: "Слияние списков")
# У тебя есть два списка:
# list_one = [1, 2, 3]
# list_two = [4, 5, 6]
# Твоя задача — создать новый список с названием merged_list, который должен содержать все элементы из list_one, а затем все элементы из list_two (в указанном порядке).
# Реши задачу оператором сложения +.
# Выведи получившийся список merged_list на экран.

list_one = [1, 2, 3]
list_two = [4, 5, 6]
merged_list = list_one + list_two
print(merged_list)

# Задача №4 (Раздел: "Добавление элементов в список и их замена")
# Дан список: animals = ["кошка", "собака", "хомяк"]
# Добавь в конец этого списка строку "попугай".
# Замени второй элемент списка (элемент с индексом 1) на строку "рыбка".
# Выведи измененный список animals на экран.

animals = ["кошка", "собака", "хомяк"]
animals.append("попугай")
animals[1] = "рыбка"
print(animals)


# Задача №5 (Раздел: "Срезы списка")
# Дан список: numbers = [10, 20, 30, 40, 50, 60, 70, 80]
# Используя срезы (slice), создай новые списки на основе numbers:
# Список first_part, содержащий первые три элемента списка numbers (с индекса 0 до 2 включительно).
# Список middle_part, содержащий элементы с индекса 3 до 5 (включительно).
# Список last_two, содержащий последние два элемента списка numbers.
# Выведи на экран все три новых списка: first_part, middle_part и last_two.


numbers = [10, 20, 30, 40, 50, 60, 70, 80]
first_part = numbers [:3]
middle_part = numbers[3:6]
last_two = numbers[-2:]
print(first_part, middle_part, last_two)

# Задача №6 (Раздел: "Добавление элементов в конкретное место списка")
# Дан список: fruits = ["яблоко", "банан", "апельсин"]
# Добавь строку "киви" в начало списка (на позицию с индексом 0).
# Добавь строку "манго" в середину списка, а именно между "банан" и "апельсин" (на позицию с индексом 3).
# Подсказка: после первой вставки список изменится, пересчитай индексы.
# Выведи итоговый список fruits на экран.

fruits = ["яблоко", "банан", "апельсин"]
fruits.insert(0, "киви")
fruits.insert(3, "манго")
print(fruits)

# Задача №7 (Раздел: "Получение элементов списка")
# Дан список: data = ["Python", 100, 3.14, True, "список"]
# Выведи на экран (каждый на новой строке или в отдельном print):
# Второй элемент этого списка (по порядку, считая с 1).
# Четвертый элемент этого списка.
# Первый и последний элементы списка в одной строке через дефис, например: Первый-Последний. Используй обращение по индексам и конкатенацию строк (или f-строку).
# Подсказка: помни, что индексация начинается с 0, а элементы могут быть разных типов.

data = ["Python", 100, 3.14, True, "список"]
elements = f"{data[0]}-{data[-1]}"
print(data[1])
print(data[3])
print(elements)


# Задача №8 (Раздел: "Замена элементов в списке")
# Дан список: transport = ["самолет", "поезд", "автобус", "такси", "велосипед"]
# Выполни следующие замены в списке transport:
# Замени элемент с индексом 2 на строку "метро".
# Замени последний элемент списка на строку "самокат" (используй отрицательную индексацию).
# Замени первый элемент списка на строку "корабль".
# Выведи итоговый список transport на экран.

transport = ["самолет", "поезд", "автобус", "такси", "велосипед"]
transport[2] = "метро"
transport[-1] = "самокат"
transport[0] = "корабль"
print(transport)

# Задача №9 (Раздел: "Создание списка и слияние списков")
# Создай пустой список с именем shopping_cart.
# Создай еще два списка:
# fruits = ["яблоки", "бананы"]
# vegetables = ["морковь", "огурцы"]
# Добавь все элементы из списка fruits в конец списка shopping_cart. Используй метод для добавления нескольких элементов (не append для каждого элемента по отдельности).
# Добавь все элементы из списка vegetables в конец списка shopping_cart тем же методом.
# Выведи итоговый список shopping_cart на экран.

fruits = ["яблоки", "бананы"]
vegetables = ["морковь", "огурцы"]
shopping_cart = []
shopping_cart.extend(fruits)
shopping_cart.extend(vegetables)
print(shopping_cart)